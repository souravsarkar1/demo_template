import dotenv from 'dotenv';

dotenv.config();

const {
    LOG_LEVEL,
    NODE_ENV = 'production',
    LOG_DIR = './logs',
    MONGODB_URI,
    DATABASE_NAME,
    DATABASE_USER_NAME,
    DATABASE_PASSWORD,
    DATABASE_HOST,
    DATABASE_PORT,
    GUPSHUUP_API_KEY,
    GUPSHUUP_TEMPLATE_API,
    HEY_REPLY_TEMPLATE_ID,
    GUPSHUP_APP_NAME,
    GUPSHUP_SOURCE_NUMBER,
    FROM_FRONTEND_SERVER_COD,
    FROM_FRONTEND_SERVER_EXPENSE,
    FRONTEND_URI,
    AWS_S3_REGION,
    AWS_S3_BUCKET,
    AWS_S3_SECRET_KEY,
    AWS_S3_ACCESS_KEY,
    VIEW_MEDIA_AWS_URL_KEY,
    OPEN_AI_KEY,
    OPEN_AI_MODEL_NAME,
    MULTIPLE_CTA_REPLY_TEMPLATE_ID,
    JWT_SECRET,
} = process.env;

const PORT = parseInt(process.env.PORT);

export {
    LOG_LEVEL,
    PORT,
    MONGODB_URI,
    NODE_ENV,
    LOG_DIR,
    DATABASE_NAME,
    DATABASE_USER_NAME,
    DATABASE_PASSWORD,
    DATABASE_HOST,
    DATABASE_PORT,
    GUPSHUUP_API_KEY,
    GUPSHUUP_TEMPLATE_API,
    HEY_REPLY_TEMPLATE_ID,
    GUPSHUP_APP_NAME,
    GUPSHUP_SOURCE_NUMBER,
    FROM_FRONTEND_SERVER_COD,
    FROM_FRONTEND_SERVER_EXPENSE,
    FRONTEND_URI,
    AWS_S3_REGION,
    AWS_S3_BUCKET,
    AWS_S3_SECRET_KEY,
    AWS_S3_ACCESS_KEY,
    VIEW_MEDIA_AWS_URL_KEY,
    OPEN_AI_MODEL_NAME,
    OPEN_AI_KEY,
    MULTIPLE_CTA_REPLY_TEMPLATE_ID,
    JWT_SECRET,
};
